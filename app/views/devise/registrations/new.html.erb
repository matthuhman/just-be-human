<div class="registration-page">
  <%= bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name), class: 'new-generic-form') do |f| %>
    <%= f.text_field :username %>
    <%= f.email_field :email %>
    <%= f.text_field :first_name %>
    <%= f.text_field :last_name %>
    <%= f.date_select :birth_date %>
    <%= f.text_field :city %>
    <%= f.text_field :region %>
    <%= f.text_field :postal_code %>
    <%= f.text_field :country %>
    <%= f.password_field :password %>
    <%= f.password_field :passowrd_confirmation %>
  <% end %>
</div>






<div class="registration-page">
  <div class="jbh-standard-title">Welcome!</div>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>
    <div class="form-inputs">
      <%= f.input :username,
                  required: true,
                  placeholder: 'What would you like to go by?',
                  autofocus: true %>
      <%= f.input :email,
                  required: true,
                  placeholder: "What's your email?",
                  input_html: { autocomplete: "email" }%>
      <%= f.input :first_name,
                  placeholder: 'Enter your first name',
                  required: true %>
      <%= f.input :last_name,
                  placeholder: 'Enter your last name',
                  required: true %>
      <%= f.input :birth_date,
                  order: [:month, :day, :year],
                  start_year: Date.today.year - 110,
                  end_year: Date.today.year - 10,
                  required: true %>
      <%= f.label 'Time Zone:' %>
      <%= f.time_zone_select(:time_zone, ActiveSupport::TimeZone.us_zones, default: 'Mountain Time (US & Canada)') %>
      <%= f.input :city,
                  placeholder: 'What city do you live in?',
                  required: true %>
      <%= f.input :region,
                  placeholder: 'What state/region is that?',
                  required: true %>
      <%= f.input :postal_code,
                  placeholder: 'Enter your postal code',
                  required: true %>
      <%= f.input :country,
                  selected: 'US',
                  required: true %>
      <%= f.input :password,
                  placeholder: 'Pick a password (the more secure the better)!',
                  required: true,
                  hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  placeholder: 'Confirm your password',
                  required: true,
                  input_html: { autocomplete: "new-password" } %>
    </div>
    <div class="submit-registration-container">
      <%= f.button :submit, "REGISTER", class: "submit-registration-button" %>
    </div>
  <% end %>
</div>
