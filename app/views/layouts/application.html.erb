<!DOCTYPE html>
<html>
  <head>
    <title>Detrashers - The cleanup community</title>
    <meta name="description" content="Detrashers is the community for people who want to participate in or organize trash cleanups. Manage waivers, RSVP's, responsibilities and more, all in one place.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= include_gon %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "https://www.google.com/jsapi", "chartkick" %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
  </head>
  <body>
    <% if !(params[:action] == 'landing') %>
      <%= render 'layouts/navbar' %>
    <% end %>
    <main class='wrapper'>
      <%= render 'layouts/alerts' %>
      <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
